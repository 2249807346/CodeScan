<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera.any" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CodeScan">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 快速扫描Activity -->
        <activity
            android:name=".QuickScanActivity"
            android:exported="true"
            android:icon="@drawable/ic_icon_new_foreground"
            android:theme="@style/Theme.CodeScan" />
        
        <!-- 独立扫描Activity，直接使用扫描布局 -->
        <activity
            android:name=".ScannerActivity"
            android:exported="true"
            android:icon="@drawable/ic_scan_with_background"
            android:label="@string/title_scan"
            android:theme="@style/Theme.CodeScan" />
        
        <!-- 新的独立启动Activity，使用ic_scan_with_background.xml图标 -->
        <activity
            android:name=".NewScannerActivity"
            android:exported="true"
            android:icon="@mipmap/ic_icon_new"
            android:label="@string/title_scan"
            android:theme="@style/Theme.CodeScan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".MeActivity"
            android:exported="false"
            android:theme="@style/Theme.CodeScan" />
        
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        


    </application>

</manifest>